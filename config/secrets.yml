# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 972d883f7b245d1fd1fdbf853fcdabbd17db1b402256e27972e29c3594c5f5574489cda7cdff2725609f015937c0b4c047ee102b563ab1eea570c98c9beb4878
  GMAIL_USERNAME: 'shapeculture@gmail.com'
  GMAIL_PASSWORD: 'robburns'

test:
  secret_key_base: 45e8bf693afe9954a9081eeda517d421f65f6c36b4638e3c5373069b3b959c0e61a3a0e19d8d3c6b3421726c5d3ea8953ee6baf99dbbbb90b03a1568e74b5b5b

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  GMAIL_USERNAME: 'shapeculture@gmail.com'
  GMAIL_PASSWORD: 'robburns'
